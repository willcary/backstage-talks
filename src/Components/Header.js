import React from 'react'
import logo from ".././images/logo.png"

const Header = () => {
    return (
        <header>
            <img src={logo} alt="Backstage Talks" />
        </header>
    )
}

export default Header
